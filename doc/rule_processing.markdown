The JSON format for writing rules allows much flexibility and few
limitations. The exact order of evaluation and valid combinations
of rules are defined by the implementation, in rule.rb and
rulesupport.rb.

